날짜
-- MYSQL
1. YEAR(MYDATE) AS YEAR, MONTH(MYDATE) AS MONTH, DAY(MYDATE) AS DAY
2. DATE_FORMAT(MYDATE, '%Y-%M-%D') = '2021-02-01'
3. JOINED LIKE '2021%'
4. WHERE END_DATE >= '2022-11-01' AND START_DATE <= '2022-11-30'
5. WHERE START_DATE BETWEEN '2022-08-01' AND '2022-10-31'
-- ORACLE
1. TO_CHAR(MYDATE, 'YYYYMMDD') = '20210201'
2. WHERE END_DATE >= TO_DATE('2022-11-01', 'yyyy-mm-dd')

조인
-- MYSQL
1. FROM A RIGHT JOIN B ON A.ID = B.ID
2. FROM A JOIN B ON A.TYPE = B.TYPE AND A.DURATION = '30일 이상'
3. FROM A INNER JOIN B ON A.ID = B.ID
4. FROM A LEFT JOIN B USING (MEMBER_ID)

--ORACLE
1. FROM A INNER JOIN B USING (CAR_TYPE)
2. FROM A NATURAL JOIN B



문자 일부
-- MYSQL
AND JOINED LIKE '2021%'

제한
-- MYSQL
LIMIT 3;
--ORACLE
FETCH FIRST 3 ROWS ONLY;
